name: Download data
on: push # temporary, for testing this script
jobs:
  download-data:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: echo $GITHUB_ACTION_PATH
      - run: rm ./public/stats-history.json
      - run: node ./utils/downloadData.mjs
      - run: cat ./public/stats-history.json